openai>=1.0.0
elevenlabs>=0.2.0
python-dotenv>=1.0.0
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24.0
scipy>=1.11.0

# Optional: Local Whisper for STT (if not using OpenAI)
# openai-whisper>=20230918

# Optional: Local TTS alternatives
# TTS>=0.22.0  # Coqui TTS
pyttsx3>=2.90  # Offline TTS (now included by default)
