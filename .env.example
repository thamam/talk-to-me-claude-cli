# API Keys
OPENAI_API_KEY=your-openai-api-key-here         # For OpenAI TTS/STT
ELEVENLABS_API_KEY=your-elevenlabs-api-key-here # For ElevenLabs TTS (optional)

# Voice Provider Selection
TTS_PROVIDER=elevenlabs  # Options: elevenlabs, openai, local

# Provider-Specific Voice Settings

# ElevenLabs Voices (highest quality, most natural)
# Options: adam, rachel, domi, bella, antoni, elli, josh, arnold, callum, charlie
ELEVENLABS_VOICE=adam  # Default for ElevenLabs

# OpenAI Voices (high quality)
# Options: alloy, echo, fable, nova, onyx, shimmer, ash, sage, coral
OPENAI_VOICE=nova  # Default for OpenAI

# TTS Speed (OpenAI only, ElevenLabs uses stability/similarity settings)
TTS_SPEED=1.0  # Speed multiplier (0.25 to 4.0, OpenAI only)

# STT Settings
STT_PROVIDER=openai  # Options: openai, local
STT_MODEL=whisper-1  # OpenAI Whisper model

# Narration Settings
NARRATION_ENABLED=true  # Enable/disable voice narration
NARRATION_VERBOSITY=medium  # Options: brief, medium, detailed
AUTO_SPEAK=true  # Automatically speak narration (false = manual trigger)

# Local TTS Settings (if using local provider)
# LOCAL_TTS_ENGINE=coqui  # Options: coqui, pyttsx3
# COQUI_MODEL=tts_models/en/ljspeech/tacotron2-DDC

# Audio Settings
AUDIO_FORMAT=mp3  # Options: mp3, opus, aac, flac
SAMPLE_RATE=24000  # Audio sample rate
